---
description: Search TormentNexus L2 memory for relevant context across all previous sessions.
---

# Search TormentNexus (/tn-search)

When I say "tn-search" or "search TN for", help me find relevant context from TormentNexus L2 memory.

1. **Extract query** — Identify the key terms or concepts to search for
2. **Search** — Use `mcp_tormentnexus_memory_scratchpad_get` to retrieve context
3. **Present** — Show the most relevant results with their tags and timestamps
4. **Apply** — Use the context to inform the current task

Example: `/tn-search pattern for error handling in API routes`
