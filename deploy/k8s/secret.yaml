apiVersion: v1
kind: Secret
metadata:
  name: marketing-agent-secrets
  namespace: tormentnexus
type: Opaque
stringData:
  # Values must be base64 encoded when using `data:`, but `stringData` accepts plain text.
  # Replace these with actual secrets in production or use an external secret store.
  DATABASE_URL: "postgres://user:pass@db-host:5432/marketing_agent?sslmode=disable"
  SECRET_KEY: "01234567890123456789012345678901"
  GITHUB_TOKEN: "github_pat_xxx"
  GITHUB_WEBHOOK_SECRET: "my_webhook_secret"
  CRM_BASE_URL: "https://api.salesforce.com"
  CRM_API_KEY: "crm_secret_key"
  HERMES_API_URL: "https://api.hermes.com/v1"
  HERMES_API_KEY: "hermes_secret_key"
  DEEPSEEK_API_KEY: "deepseek_secret_key"
  HUNTER_API_KEY: "hunter_secret_key"
  APOLLO_API_KEY: "apollo_secret_key"
  SMTP_USERNAME: "smtp_user"
  SMTP_PASSWORD: "smtp_password"
  IMAP_USERNAME: "imap_user"
  IMAP_PASSWORD: "imap_password"
  STRIPE_API_KEY: "sk_live_xxx"
  STRIPE_WEBHOOK_SECRET: "whsec_xxx"
  OUTBOUND_WEBHOOK_URL: "https://example.com/webhook"
  OUTBOUND_WEBHOOK_SECRET: "outbound_secret"
  LINKEDIN_USERNAME: "linkedin_user"
  LINKEDIN_PASSWORD: "linkedin_password"
  REDIS_URL: "redis://redis-host:6379/0"
