apiVersion: v1
kind: Service
metadata:
  name: {{ include "marketing-agent.fullname" . }}
  labels:
    {{- include "marketing-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "marketing-agent.selectorLabels" . | nindent 4 }}
